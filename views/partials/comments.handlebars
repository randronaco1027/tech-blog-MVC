<div>
    {{#each this}}
    <section>
        <div>
            {{user.username}} on {{createdAt}}
        </div>
        <div>
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>